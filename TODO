TODO List:

[X] Automatisation du déploiement (init.sh, deploy.sh full/code)
[X] Documentation (README racine, rootkit/README, attacking_program/README)

[X] Fonctionnalités — état actuel :

    CONNEXION / AUTH
    [X] ping        : OK
    [X] auth        : OK
    [X] exit        : OK
    [X] help        : OK

    EXÉCUTION
    [X] exec        : OK — mode one-shot et mode interactif avec suivi du répertoire courant (cd persistant)

    TRANSFERT
    [X] upload      : OK
    [X] download    : À tester (non validé)

    HIDING
    [X] hide_module : OK — module absent de lsmod et /proc/modules
    [X] hide_file   : OK — hook getdents64 fonctionnel sur kernel 5.10 cloud-amd64
                          (signature const struct pt_regs * correcte pour CONFIG_ARCH_HAS_SYSCALL_WRAPPER)
    [X] unhide_file : OK — testé conjointement avec hide_file
    [ ] hide_line   : NON ACTIVÉ — le hook read() est présent dans le code mais non installé
                      dans la syscall table (trop risqué : déclenche sur chaque read() du système).
                      À réactiver avec précaution et tests dédiés.
    [ ] unhide_line : NON TESTÉ (bloqué par hide_line non activé)

    PERSISTANCE
    [X] persistence : OK — service systemd installé, rootkit rechargé au boot

TODO List:

[X] Automatisation du déploiement (init.sh, deploy.sh full/code)
[X] Documentation (README racine, rootkit/README, attacking_program/README)

[ ] Fonctionnalités — état actuel :

    CONNEXION / AUTH
    [X] ping        : OK
    [X] auth        : OK
    [X] exit        : OK
    [X] help        : OK

    EXÉCUTION
    [X] exec        : OK — mode one-shot et mode interactif avec suivi du répertoire courant (cd persistant)

    TRANSFERT
    [X] upload      : OK
    [ ] download    : À tester (non validé)

    HIDING
    [X] hide_module : OK — module absent de lsmod et /proc/modules
    [ ] hide_file   : KO — le hook syscall est installé (syscall_table patché via lookup_address)
                          mais le filtrage échoue sur kernel 6.1 genericcloud-amd64 :
                          CONFIG_ARCH_HAS_SYSCALL_WRAPPER place un pt_regs* dans le premier
                          argument, le vrai pointeur dirent n'est pas encore extrait correctement.
    [ ] unhide_file : NON TESTÉ
    [ ] hide_line   : NON TESTÉ (même blocage que hide_file)
    [ ] unhide_line : NON TESTÉ

    PERSISTANCE
    [X] persistence : OK — service systemd installé, rootkit rechargé au boot

[ ] Problème ouvert : hide_file/hide_line sur image genericcloud (kernel 6.1.0-*-cloud-amd64)
    Solution possible : changer DEBIAN_CLOUD_URL dans .env pour une image netinst standard.

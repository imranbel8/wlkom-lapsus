#include <criterion/criterion.h>

#include "../include/commands.h"

#include <stdio.h>
#include <stdlib.h>
#include <string.h>

Test(commands_basics, dispatch_help)
{
	cr_skip("Command dispatch requires full client setup", 0);
}

Test(commands_basics, dispatch_ping)
{
	cr_skip("Command dispatch requires full client setup", 0);
}

Test(commands_basics, dispatch_exec)
{
	cr_skip("Command dispatch requires full client setup", 0);
}

Test(commands_basics, dispatch_upload)
{
	cr_skip("Command dispatch requires full client setup", 0);
}

Test(commands_basics, dispatch_download)
{
	cr_skip("Command dispatch requires full client setup", 0);
}
